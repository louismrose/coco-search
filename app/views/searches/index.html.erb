<h1>Searches</h1>

<% if @searches %>
  <ul>
  <% @searches.each do |search| %>
    <li><%= link_to "Search #{search.id}", search %></li>
  <% end %>
  </ul>

<% else %>
  No searches have been created yet.
<% end %>

<%= link_to "Create a new search", new_search_path %>
